export class User {
    constructor(public firstName: string, public lastName: string, public username: string, public gender: string
        , public email: string, public password: string, public phone: string
    ) { }
}